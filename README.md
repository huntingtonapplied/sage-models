# sage-models

Runnable inference package for sage. `pip install sage-models`, then `from sage_models import load`.

Closed-source, compiled distribution. Licensed under the AHL Model EULA (see LICENSE).
